# InfraGenie frontend environment — copy to frontend/.env and fill in.

# Fully-qualified backend URL (same origin recommended)
REACT_APP_BACKEND_URL=https://your-preview-domain.example.com
WDS_SOCKET_PORT=443
ENABLE_HEALTH_CHECK=false
